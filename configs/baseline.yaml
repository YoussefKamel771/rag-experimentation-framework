input_dir: data/raw/text_data_toc.csv
output_dir: artifacts

chunking:
  strategy: recursive
  params:
    chunk_size: 1000
    chunk_overlap: 150

embedding:
  provider: ollama
  params:
    model: embeddinggemma:300m
    base_url: ollama

vector_store:
  type: faiss
  params: {}
  